<!--pages/professorInfo/professorInfo.wxml-->
<view>
  <Header></Header>
  <view class="title">
    <view class="left">
      {{professorName}}
    </view>
    <view class="middle"></view>
    <view class="right">{{overallRating.toFixed(2)}}</view>
  </view>
  <view class="courseInfo">
    <view class="info">
      <view class="courseTitle">Courses taught</view>
    </view>
    <view class="unit" wx:for="{{courseTaught}}" wx:key="index" > <text>{{item.courseCode}}</text>: {{item.courseUnit}} units</view>
  </view>
  <!-- numeric Rating with selection box and four ratings -->
  <NumericRating dropDownType="{{1}}" interestRating="{{interestRating}}" difficultyRating="{{difficultyRating}}" workloadRating="{{workloadRating}}" teachRating="{{teachingRating}}" professorID="{{professorID}}" bindchoosePicker="handlePicker"></NumericRating>
  <!-- display user ratings  -->

<Rating wx:for="{{reviews}}" item="{{item}}" hideProfessor="{{true}}"></Rating>


  <!-- <YellowButton text="更多评价" bindtap="moreRating"></YellowButton> -->
  <Pagination pages="{{totalPage}}" bind:onClickItem="handlePagination" activePage="{{currentPageInReviews}}"></Pagination>
  <BottomNav></BottomNav>
</view>
